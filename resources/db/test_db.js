// import mongoose from "mongoose";

// const testDB = await mongoose.connect(`${process.env.MONGODB_TEST_URL}`);

// testDB.on("error", console.error.bind(console, "connection error:"));
// testDB.once("open", () => {
//   console.log("connected");
// });

// export default testDB;
